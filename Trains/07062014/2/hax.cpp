#include <cstdio>

typedef long long LL;

int main() {
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);

    int n, k;
    scanf("%d%d", &n, &k);

    for (int i = 1; i < n; ++i) {
        scanf("%d%d%d", &y, &t, &)
    }
    LL sum = 0;
    for (int i = 0; i < n; ++i) {
        sum += LL(s) * s;
    }
    if (LL(l) * l > sum)
        puts("NO");
    else
        puts("YES");
    return 0;
}
